def commaCode(alpha):
	for x in range(len(alpha)-1):
		print(f"{alpha[x]}", end='')
	print(f'and {alpha[x+1]}')
	
beta = []

while True:
	gamma = input('Type your item')
	if gamma != '':
		beta.append[item]
	else:
		break

commaCode(beta)
